{
  "name": "invoice-backend",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Headless invoice generation. Method A: JSON -> PDF (pdfmake, no LibreOffice). Method B: .docx template -> validated multi-format output (docxtemplater + LibreOffice).",
  "scripts": {
    "gen": "node src/cli.ts",
    "make-template": "node src/make-template.ts",
    "render": "node src/render.ts"
  },
  "dependencies": {
    "docx": "^9.0.0",
    "docxtemplater": "^3.50.0",
    "pdfmake": "^0.2.10",
    "pizzip": "^3.1.7"
  }
}
